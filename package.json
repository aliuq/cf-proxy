{
  "name": "proxy-github",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.18.0",
    "typescript": "^4.8.4",
    "wrangler": "2.1.15"
  },
  "private": true,
  "scripts": {
    "dev": "wrangler dev -l --ip localhost",
    "deploy": "wrangler publish"
  }
}
