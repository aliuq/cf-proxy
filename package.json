{
  "name": "proxy-github",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.16.0",
    "typescript": "^4.8.4",
    "wrangler": "2.1.9"
  },
  "private": true,
  "scripts": {
    "dev": "wrangler dev -l --ip localhost",
    "deploy": "wrangler publish"
  }
}
