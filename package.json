{
  "name": "proxy-github",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev -l --ip localhost",
    "deploy": "wrangler publish",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@vespaiach/axios-fetch-adapter": "^0.3.1",
    "axios": "^0.26.0",
    "openai": "^3.1.0"
  },
  "devDependencies": {
    "@aliuq/eslint-config": "^0.0.5",
    "@cloudflare/workers-types": "^3.18.0",
    "eslint": "^8.29.0",
    "typescript": "^4.8.4",
    "wrangler": "2.1.15"
  }
}
